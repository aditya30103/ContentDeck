# v3.7 — Mobile UX Redesign

**Date:** 2026-03-15
**Branch:** `feat/mobile-ux-redesign`
**PR:** pending
**Tests:** 267 passing

## Summary

Mobile UX overhaul: 100dvh layout foundation, responsive form inputs (text-base to prevent iOS zoom), pull-to-refresh with Framer Motion, and draggable bottom sheet on DetailPanel. All gestures use Framer Motion for smooth spring animations. Total: 5 commits, 0 breaking changes.

## Features Shipped

1. **CSS Foundation (Task 1)** — 100dvh height fix, body overflow lock, input font-size floor (text-base)
2. **App.tsx Layout (Task 2)** — Replace min-h-screen with min-h-[100dvh]
3. **AppShell Scroll Behavior (Task 3)** — Set overscroll-behavior: contain on main content area
4. **MobileHeader Button Reduction (Task 4)** — Reduce to 3 buttons (Search + Settings + Add)
5. **HomePage Audit (Task 5)** — 100dvh layout + overscroll-contain verification
6. **Form Input Audit (Task 6)** — Fix remaining text-sm on form elements (TagAreaInput, SortSelect, FeedbackList)
7. **Framer Motion Library (Task 7)** — npm install + typecheck
8. **Pull-to-Refresh (Task 8)** — usePullToRefresh hook with 64px threshold, 0.5 resistance curve, AppShell integration
9. **DetailPanel Drag Gesture (Task 9)** — Framer Motion draggable bottom sheet with spring animation, visual drag handle

## Key Implementations

### usePullToRefresh Hook
- Tracks touch events on container
- 64px threshold with 0.5 resistance curve
- Spring animation via Framer Motion
- Triggers queryClient.invalidateQueries() on release
- 5 unit tests covering drag distance, threshold, and callback

### DetailPanel Improvements
- Replaced CSS slideUp animation with Framer Motion
- Draggable y-axis with elastic feedback
- Smart dismiss: 40% panel height OR velocity > 500px/s
- Visual drag handle pill at top
- AnimatePresence for smooth mount/unmount
- Animated backdrop opacity via useTransform

### Form Input Fixes
- TagAreaInput.tsx line 197: text-sm → text-base
- SortSelect.tsx line 18: text-sm → text-base
- FeedbackList.tsx: status select + resolution input both updated to text-base + min-h-[44px]

## Commits

1. `fix: ensure all form inputs use text-base (16px) to prevent iOS auto-zoom`
2. `chore: install framer-motion for animation library`
3. `feat: add pull-to-refresh gesture on mobile`
4. `feat: add Framer Motion drag-to-dismiss to DetailPanel`
5. (Final) Fix TypeScript/lint errors before PR

## QA Checklist

- [x] All 267 tests passing
- [x] ESLint: 0 errors, 5 pre-existing warnings
- [x] TypeScript strict mode: clean
- [x] Prettier: formatted
- [x] Production build: success (894.78 kB)
- [x] iOS auto-zoom on inputs: prevented (all form elements ≥ 16px)
- [x] Pull-to-refresh: works at 100dvh (100vh + safe-area on mobile)
- [x] DetailPanel drag gesture: smooth spring physics

## Performance

- Bundle size: 894.78 kB (gzip: 267.08 kB)
- No new dependencies beyond framer-motion
- Zero breaking changes to existing APIs
- All gestures use efficient Framer Motion primitives (useMotionValue, useTransform)

## Accessibility

- Keyboard support: ESC closes DetailPanel
- Touch targets: all interactive elements ≥ 44×44px
- ARIA labels on drag handle and gestures
- Motion-safe: animations respect prefers-reduced-motion

## Next Steps

Tasks 10, 11, 12 remain for future sessions:
- Task 10: Modal.tsx drag-to-dismiss base + AnimatePresence
- Task 11: ReaderModal swipe-right-to-close
- Task 12: AnimatePresence route transitions
